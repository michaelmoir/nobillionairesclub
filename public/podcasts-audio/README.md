Place local podcast audio files in this folder if you want NAS-hosted files.

Suggested naming:
- NBC0001.mp3
- NBC0002.mp3

These files are gitignored by default to keep repository size small.

If using local files, point `audioUrl` in `src/content/podcasts.ts` to:
- /podcasts-audio/<filename>.mp3
